# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is an Odoo-related Python project in its initial setup phase. Currently it contains only a Python virtual environment.

## Environment Setup

- **Python version**: 3.13 (via miniconda3)
- **Virtual environment**: `abdo_env/` (created with `python -m venv`)
- **Activate**: `source abdo_env/bin/activate`
- **Install dependencies**: `pip install <package>` (no requirements file exists yet)
